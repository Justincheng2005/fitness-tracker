/*import express from 'express';
import { getLifts } from '../controllers/liftController.js';
import { getLift } from '../controllers/liftController.js';
import { createLift } from '../controllers/liftController.js';
import { deleteLift } from '../controllers/liftController.js';
import { editLift } from '../controllers/liftController.js';


const router=express.Router();

router.get('/', getLifts);

router.get('/:id', getLift)

router.post('/', createLift);

router.delete('/:id', deleteLift);

router.patch('/:id', editLift);

export default router;*/